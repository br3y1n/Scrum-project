@model IEnumerable<GestorDeActividades.Models.ROLES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Cargo o Rol
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rol_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.rol_code }) |
            @Html.ActionLink("Details", "Details", new { id=item.rol_code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.rol_code })
        </td>
    </tr>
}

</table>
