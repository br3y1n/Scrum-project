@model IEnumerable<GestorDeActividades.Models.ACTIVITY>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Actividad
        </th>
        <th>
            Fecha de Entrega
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.act_description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.act_date_delivery)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.act_code }) |
            @Html.ActionLink("Details", "Details", new { id=item.act_code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.act_code })
        </td>
    </tr>
}

</table>
